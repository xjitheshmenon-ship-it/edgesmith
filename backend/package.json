{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development node --watch app.js",
    "start": "NODE_ENV=production node app.js",
    "migrate": "node scripts/migrate.js",
    "seed": "node seeds/seed.js",
    "validate-schema": "node scripts/validate_schema.js",
    "test": "echo \"No automated test suite yet — see scripts/validate_schema.js and the manual HTTP smoke checks in the build notes\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "node-cron": "^4.5.0",
    "pg": "^8.22.0",
    "pg-format": "^1.0.4"
  },
  "devDependencies": {
    "pgsql-parser": "^17.9.15"
  }
}
